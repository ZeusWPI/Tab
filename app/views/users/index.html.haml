%h2 Users
%table{ class: "table-responsive" }
  %thead
    %tr
      %th ID
      %th Name
      %th Balance
  %tbody
    - @users.each do |u|
      %tr
        %td= u.id
        %td= u.name
        %td= u.balance
